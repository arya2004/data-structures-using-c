#include<stdio.h>
#include<conio.h>
#include<stdlib.h>
#include <math.h>

typedef struct IThreadedBinaryTree{
    char _data;
    int lbit, rbit;
    struct IThreadedBinaryTree *left, *right;
}ThreadeddBinaryTree;

int main() {
     
    int values[] = {1,2,3,4,5,6,7,9,8};
    int numValues = sizeof(values) / sizeof(values[0]);

     //root = AddNode(root, 44);



    
    return 0;
}