#include <stdio.h>
#include "bfs_dfs.h"

/**
 * Main function to demonstrate BFS and DFS algorithms.
 */
int main() {
    printf("BFS: ");
    BFS();

    printf("\nDFS: ");
    DFS();

    return 0;
}
